eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8080/eureka/

  server:
    waitTimeInMsWhenSyncEmpty: 0
  instance:
    preferIpAddress: true
    leaseRenewalIntervalInSeconds: 5

turbine:
  aggregator:
    clusterConfig: COMPUTE
  appConfig: COMPUTE